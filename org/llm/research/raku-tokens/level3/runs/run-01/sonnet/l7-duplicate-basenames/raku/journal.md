- attempt-01: recursive file walk collecting `.java`/`.kt` files under `src/main/java/`, group by basename with extension stripped via `subst(/\.[^.]+$/, '')`, filter groups with >1 member, sort by count desc then basename asc. Ran clean (exit 0, no stderr) but produced wrong output: `2	MAINBraid.java` — the basename still had its extension attached. Cause: `[^.]` inside a Raku regex is not POSIX-style negation (Raku uses `<-[.]>` for a negated character class), so the pattern never matched and the `subst` was a no-op for every file, leaving the full filename as the "basename" key in most cases (it happened to only surface as visibly wrong on the one line printed, since the reduced-attribute confusion still grouped by identical full name so the *count* was right by luck for that entry).
- attempt-02: same program, only change is the extension-stripping regex from `/\.[^.]+$/` to `/\.<-[.]>+$/`. Ran clean and produced correct-looking output: `3 duplicated` / `CroTemplateCall`, `MAINBraid`, `RakuBlock`, each count 2, sorted correctly (all tied at count 2, alphabetical). Manually verified via `find` that each of these three basenames does appear on exactly two files in `src/main/java/` (a `.java`/`.kt` pair for two of them, two `.java` files in different directories for `MAINBraid`).
