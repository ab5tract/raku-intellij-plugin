attempt-01: Initial implementation. Listed `.md` files directly in `org/llm/traces/` (non-recursive via `os.listdir`), lowercased each file's text, extracted maximal runs of `[a-z]+` with length >= 4 via regex, discarded stopwords, counted, and printed the top 12 by count descending / word ascending. Ran clean on the first try: exit code 0, empty stderr, 12 tab-separated lines each terminated with a newline. Output judged correct; no further attempts needed.
