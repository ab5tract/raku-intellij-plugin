attempt-01: Initial implementation. Listed `.xml` files directly in `src/main/resources/META-INF/` (non-recursive, `os.listdir` + `isfile` check), regex-matched `implementation="([^"]*)"` across each file's full text, collected distinct values in a set, sorted ascending by full value, and printed `<N> distinct` followed by `<simple>\t<full>` lines (simple name = substring after last `.`, or whole value if no `.`). Ran clean on the first try: 80 distinct values, output sorted correctly, counts and uniqueness checked out, stderr empty. No further attempts needed.
