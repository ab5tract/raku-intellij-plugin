attempt-01: Walk `src/main/java` recursively for `*.java` files, scan each line for the exact prefix `import ` and suffix `;` (no leading-whitespace stripping, per spec), extract and trim the name between them, tally with a Counter, sort by (-count, name) for descending count / ascending name tiebreak, take top 10, and print 10 `count<TAB>name` lines plus a final `TOTAL<TAB>n` line. Ran clean on the first try: exit 0, no stderr, 11 lines of output each newline-terminated, ending in `TOTAL\t5405`. No issues found, so no attempt-02 was needed.
