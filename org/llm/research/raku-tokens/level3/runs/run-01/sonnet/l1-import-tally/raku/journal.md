attempt-01: Recursively walk `src/main/java/`, collect `.java` files, scan lines for exact `import ` prefix and `;` suffix (no trimming before the check), extract and trim the name, tally counts, sort by name ascending then stably by count descending, print top 10 plus TOTAL line. Ran clean on the first try: exit 0, no stderr, exactly 11 tab-separated lines each newline-terminated. No further attempts needed.
