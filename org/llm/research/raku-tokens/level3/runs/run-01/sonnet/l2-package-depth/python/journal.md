- attempt-01: Walk `src/main/java` for `.java` files, scan each for the first line starting with `package ` and ending with `;`, tally depth (dot-segment count) or count as `none`. Ran clean on the first try; output summed to 843 (matching total file count) and spot-checking the 3 `none` files confirmed they only had commented-out `//package ...` lines, which correctly fail the "starts with exactly `package `" check. No changes needed.
