attempt-01: Recursively walk `src/main/java/`, collect `.java`/`.kt` files, count lines containing `TODO` or `FIXME` (case-sensitive) per file, emit `<count>\t<path>` for files with count >= 1 sorted by count descending then path ascending, followed by `TOTAL\t<n>`. Ran clean on the first try — no crash, empty stderr. Spot-checked the sort order and case-sensitive matching by inspection; output judged correct, so no attempt-02 was needed.
