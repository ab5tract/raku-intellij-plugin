attempt-01: First version. Scans `src/main/resources/META-INF/*.xml` (non-recursive) with the regex `'<' (<[A..Za..z]> <[A..Za..z0..9\-_.:]>*)`, tallies tag names in a Hash, sorts by count descending then name ascending, prints top 10 plus a `DISTINCT` line. Ran clean on the first try: exit 0, empty stderr, exactly 11 lines each newline-terminated. Output looked plausible on inspection (e.g. commented-out `<action>`/`<add-to-group>` tags in actions.xml were correctly still counted, per the spec's instruction not to exclude matches inside comments). No further attempts needed.
