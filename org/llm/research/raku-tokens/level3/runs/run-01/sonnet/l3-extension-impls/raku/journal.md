- attempt-01: Initial implementation. Globs `*.xml` files directly in `src/main/resources/META-INF/` (non-recursive via `.IO.dir.grep`), extracts `implementation="VALUE"` via regex match with `:g`, dedupes in a Hash, sorts ascending by full value, and prints `<simple><TAB><full>` after a `<N> distinct` header line. Ran clean on first try: exit 0, empty stderr, 81 output lines (1 header + 80 distinct values), trailing newline present, `meta/` subdirectory correctly excluded. Judged correct; no further attempts needed.
