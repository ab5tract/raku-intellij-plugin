attempt-01: Read files directly in `org/llm/traces/` ending in `.md`, lowercased each, extracted maximal runs of `<[a..z]>` with `** 4..*` (greedy, so naturally maximal) via `.match(:g)`, dropped stopwords with a Set lookup, tallied into a Hash, sorted by count descending then word ascending with an explicit `<=>`/`cmp` comparator, took the top 12, printed `count<TAB>word`. Ran clean on the first try: exit 0, empty stderr, 12 output lines each newline-terminated. Verified the regex's maximal-run and length-4 threshold behavior against a synthetic string (not the real input) before accepting the result. No further attempts needed.
