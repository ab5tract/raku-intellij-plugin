attempt-01: os.walk over `src/main/java`, filter `.java`/`.kt` names, count lines containing `TODO` or `FIXME` (case-sensitive substring), keep files with count >= 1, sort by count desc then path asc, print `<count>\t<path>` lines followed by `TOTAL\t<n>`. Ran clean on first try (exit 0, empty stderr); spot-checked the top file (`RakuFormattingModelBuilder.kt`, count 3) against a manual grep and it matched. No further attempts needed.
