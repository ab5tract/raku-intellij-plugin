attempt-01: os.walk over src/test/kotlin for *.kt files, read each as UTF-8 text, split on "\n" (universal-newline translation in text mode already normalizes \r\n and \r), take max line length (0 for empty file via the default in max(..., default=0)), sort by (-maxlen, path), print top 5 as "<maxlen>\t<path>\n". Ran clean on the first try: exit 0, empty stderr, output is exactly 5 tab-separated lines sorted descending by length with distinct maxlen values (1028, 843, 289, 239, 179), all paths correctly using "/" separators relative to repo root. No issues found; no further attempts needed.
