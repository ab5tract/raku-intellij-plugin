attempt-01: First implementation. Walks `src/main/java` recursively for `*.kt` files, splits each file's text on `\n`, drops a trailing empty piece if present, counts blank vs non-blank lines, and computes the mean with `decimal.Decimal.quantize(..., ROUND_HALF_UP)` for half-away-from-zero rounding to 2 decimals. Ran clean on the first try: exit 0, empty stderr, output `files 253 / lines 18454 / nonblank 16000 / mean 63.24`, matching the required tab-separated 4-line format. No further attempts needed.
