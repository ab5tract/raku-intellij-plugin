- attempt-01: Recursively walk `src/test/kotlin/` for `.kt` files, compute each file's longest line length via `.lines.map(*.chars).max`, sort by `(-maxlen, path)` using Raku's lexicographic List `cmp`, print top 5 as `<maxlen><TAB><path>`. Ran clean on the first try: exit 0, empty stderr, exactly 5 output lines each newline-terminated, plausible ordering (top result is a 1028-char line in a 73KB annotation test file). No changes needed.
